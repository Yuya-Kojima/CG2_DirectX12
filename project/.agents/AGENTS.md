<RULE[user_global]>
## 【絶対遵守】実装前の確認・検証の徹底と「思い込み」の排除

1. **思い込みの排除（コンテキストの確認）**
   - 一般的なゲーム（アクションRPGなど）の構造（例：Y=0の床が存在する等）を勝手に前提としないこと。
   - レールシューターなど、対象プロジェクトの実際の仕様やジャンルを正しく理解した上でコードを書くこと。

2. **既存仕様の事前調査（推測でのコーディング禁止）**
   - 新しい処理を書く前に、必ず対象のヘッダファイル（クラス仕様）や既存変数がどこで初期化・使用されているかをツール(`view_file` 等)を用いて調査すること。
   - 例: `SetTranslation` した後に `Update()` が必要かどうかも推測せず、必ずクラスの定義を確認する。

3. **自己検証の義務（ノーテストでの報告禁止）**
   - コードを書いた後は必ず脳内でトレースを行うこと。
   - 「絶対にエラーは起きない」と報告する前に、必ず自ら `run_command` を用いて手元の環境でMSBuildによるコンパイルを実行し、エラー(0 errors)が出ないことを物理的に確認すること。

4. **【絶対遵守】ユーザーの明確な許可なきファイル変更・書き換えの完全禁止**
   - AIは過去に何度も「許可なく勝手にコードやコメントを変更する」という重大なルール違反を犯している。この事実を重く受け止め、行動を根本から修正すること。
   - ユーザーが「○○のコードを修正して」「この変更を適用して」と**明確かつ直接的な実行指示**を出さない限り、`replace_file_content` や `multi_replace_file_content`、`write_to_file` 等のファイル変更ツールを**絶対に呼び出してはならない**。
   - 解決策や修正案が見つかった場合でも、コードの書き換えツールを実行する前に、必ず「このように修正しますが、適用してよろしいでしょうか？」とユーザーに確認し、明確な承認を得ること。
   - 「単なるコメントの修正」「明らかなタイポの修正」であっても、このルールの例外とはならない。一切の独断専行を禁ずる。
</RULE[user_global]>
